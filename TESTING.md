# سجل التحقق من المشروع بعد إضافات Lab 5

## الإعداد الحالي: 20 مرشحًا، ومصدران للإجابة، وتعليمات امتناع معدّلة — 22 سبتمبر 2026

يسترجع E5 الآن `RERANK_POOL = 20` مصدرًا، ويعيد Cross-Encoder ترتيبها، ونحتفظ بأفضل `CANDIDATE_K = 5`، ويقرأ Qwen أفضل `TOP_K = 2`. «قبل» = أول خمسة من E5، و«بعد» = أول خمسة بعد إعادة الترتيب. شُغّل `python validate_project.py` بالنماذج الفعلية على CPU (308 ثوانٍ)، ونجح `python validate_lab6.py`.

| المقياس — 8 أسئلة | قبل إعادة الترتيب | بعد إعادة الترتيب |
|---|---:|---:|
| Hit@1 | 0.625 (5/8) | 0.875 (7/8) |
| Recall@5 | 0.875 | 1.000 |
| MRR@5 | 0.729 | 0.938 |

| الشريحة | الأسئلة | مصدر أول صحيح قبل | مصدر أول صحيح بعد |
|---|---:|---:|---:|
| جدول | 3 | 1/3 | 2/3 |
| نص سردي | 5 | 4/5 | 5/5 |
| سؤال مباشر | 6 | 4/6 | 5/6 |
| صياغة بديلة | 2 | 1/2 | 2/2 |

**كيف اخترنا الإعداد:** قبل تغيير الدفتر قارنّا الاسترجاع وحده على الأسئلة الثمانية نفسها:

| الإعداد | Hit@1 | Recall@5 | MRR@5 |
|---|---:|---:|---:|
| إعادة ترتيب 5 (السابق) | 0.750 | 0.875 | 0.812 |
| إعادة ترتيب 20 | 0.875 | 1.000 | 0.938 |
| إعادة ترتيب 50 | 0.875 | 0.938 | 0.938 |
| BM25 مع E5 (RRF) ثم إعادة ترتيب 20 | 0.875 | 1.000 | 0.938 |
| إزالة تكرار عنوان النافذة الأولى | 0.750 | 0.875 | 0.812 |

اخترنا 20 لأن BM25 لم يضف تحسنًا على هذه العينة، وإزالة تكرار العنوان لم تغيّر شيئًا. الاسترجاع مع إعادة الترتيب يستغرق نحو 1.9 ثانية للسؤال بدل 0.4 ثانية على CPU. جرّبنا ثلاثة أحجام واخترنا الأفضل على ثمانية أسئلة فقط؛ قد يكون التحسن على أسئلة جديدة أقل.

**التوليد: مصدران وتعليمات امتناع معدّلة.** أضفنا إلى Prompt: «إذا ذكرت المصادر خطوات أو شروطًا أو مدة أو رسومًا أو وثائق تخص السؤال فانقلها كما وردت، دون إضافة بنود غير مذكورة»، وصار الامتناع «فقط إذا لم تتناول المصادر موضوع السؤال». قبل تغيير الدفتر ولّدنا إجابات الأسئلة العشرة بأربعة إعدادات، ثم راجعها المساعد مقابل نصوص المصادر (مراجعة أولية، ليست بشرية):

| الإعداد | إجابات صحيحة داخل النطاق | امتناع مناسب خارج النطاق |
|---|---:|---:|
| Prompt السابق، `TOP_K = 1` | 3/8 | 2/2 |
| Prompt المعدّل، `TOP_K = 1` | 4/8 | 2/2 |
| Prompt السابق، `TOP_K = 2` | 5/8 | 2/2 |
| Prompt المعدّل، `TOP_K = 2` (الحالي) | 6/8 | 2/2 |

إجابات التشغيل الرسمي مطابقة نصيًا لإجابات التجربة بالإعداد الحالي.

**حالة الأسئلة الآن:**

- الأسئلة 1 و2 و4 و5 و6 و8: صحيحة حسب المراجعة الأولية. السؤال 5 أجاب لأن المصدر الثاني (صفحة 6) يذكر الخانات الأربع. السؤالان 4 و8 نقلا خطوات صفحة 4 بعد أن كانا يمتنعان.
- السؤال 3: ذكر الوثائق الخمس ثم أضاف «الوثائق الأخرى المطلوبة حسب القوانين المحلية والأنظمة المعتمدة»؛ إضافة غير مدعومة.
- السؤال 7: أجاب سنة واحدة بصياغة مضطربة («تتراوح بين سنة واحدة») ودون توضيح الفرق بين الرخصتين؛ ناقصة أو ملتبسة.
- سؤالا خارج النطاق: امتناع بالعبارة المطلوبة.
- لا توجد أخطاء بحث آلية: كل مصدر مرجعي بين المرشحين، وكل سؤال مرّر مصدرًا مرجعيًا إلى Qwen.

**حدود هذه النتائج:** عدّلنا Prompt واخترنا `TOP_K` وحجم المرشحين على الأسئلة نفسها التي نقيس بها، لذلك قد يكون التحسن على أسئلة جديدة أقل. المصدر الثاني يطيل السياق: زمن توليد الإجابة الواحدة من 14 إلى 42 ثانية على CPU، وكان من 7 إلى 58 ثانية مع مصدر واحد. في تجربة سابقة بـ Prompt القديم ومصدرين أضاف النموذج شرطًا غير وارد (رقم هاتف) من المصدر الثاني؛ مصدر إضافي قد يُدخل معلومات لا تخص السؤال.

## طريقة اختيار المصادر الخمسة في `app.py` — 25 سبتمبر 2026

سؤال مستخدم: «أنا قمت بالابلاغ عن مخالفة للنظام في منشأة, كيف أعرف انه تم التحقق من الامر؟». الإجابة في الصفحة 4 (خطوات متابعة البلاغ) والصفحة 87 (الأسئلة الشائعة عن البلاغات). وضع E5 الصفحتين في المرتبتين 2 و6، ثم أنزلهما Cross-Encoder إلى 14 و11، فلم يصلا إلى المصادر الخمسة، وقرأ النموذج قوالب إشعارات بها فراغات مثل «(رقم المخالفة)» وأجاب بأنه لا يستطيع الوصول إلى «البيانات الخاصة».

قارنّا طرق اختيار الخمسة على الأسئلة الثمانية ذات المراجع، وعلى سؤال المستخدم (المرجع: الصفحتان 4 و87). الاسترجاع فقط، دون توليد.

| الطريقة | Hit@1 | Recall@2 | Recall@5 | MRR@5 | سؤال المستخدم Recall@5 |
|---|---:|---:|---:|---:|---:|
| E5 وحده | 0.62 | 0.62 | 0.88 | 0.73 | 0.5 |
| ترتيب Cross-Encoder وحده (السابق) | 0.88 | 0.94 | 1.00 | 0.94 | 0.0 |
| دمج الترتيبين (RRF، k=5 أو 10) | 0.62 | 0.81 | 1.00 | 0.78 | 0.5 |
| دمج الترتيبين (RRF، k=60) | 0.62 | 0.81 | 0.88 | 0.75 | 0.0 |
| **أول 3 من Cross-Encoder ثم الإكمال بترتيب E5 (الحالي)** | 0.88 | 0.94 | 1.00 | 0.94 | 0.5 |
| أول 4 من Cross-Encoder ثم الإكمال بترتيب E5 | 0.88 | 0.94 | 1.00 | 0.94 | 0.0 |

**القرار:** اعتمد `app.py` «أول 3 ثم E5»؛ لم يتغير أي مقياس على الأسئلة الثمانية، وعادت الصفحة 4 إلى المصادر الخمسة لسؤال المستخدم (المرتبتان 4 و5).

**الحدود:** اخترنا هذه الطريقة بعد رؤية سؤال واحد فاشل، والعينة صغيرة. الصفحة 4 في المرتبة 4، فيقرؤها Claude (خمسة مصادر) ولا يقرؤها النموذج المحلي (مصدران). الصفحة 87 ما زالت خارج الخمسة. الدفتر `rag_marimo.py` ما زال يستخدم ترتيب Cross-Encoder وحده.

## خمسة مصادر بدل مصدرين للنموذج المحلي — 25 سبتمبر 2026

شغّلنا Qwen2.5-1.5B على الأسئلة العشرة وهو يقرأ أفضل خمسة مصادر بدل مصدرين، بالبحث والـPrompt نفسيهما. الأحكام مراجعة أولية أجراها المساعد، وليست مراجعة بشرية.

| الإعداد | صحيحة داخل النطاق | امتناع مناسب خارج النطاق | زمن الإجابة |
|---|---:|---:|---|
| مصدران (الحالي) | 6/8 | 2/2 | من 14 إلى 42 ثانية |
| خمسة مصادر | 6/8 | 2/2 | من 9 إلى 70 ثانية |

- تحسّن السؤال 3: نقل الوثائق دون البند الإضافي.
- ساء السؤال 8 (متابعة البلاغ عبر HRSD): ذكر «رقم هاتف محمي بالخصوصية أو البريد الإلكتروني» وهو غير وارد في المصادر، وظهرت في الإجابة كلمة صينية.
- بقي السؤال 7 ملتبسًا.

**القرار في `app.py`:** تعرض الصفحة أفضل خمسة مصادر، ويقرأ النموذج المحلي أول مصدرين فقط، ويقرأ Claude الخمسة. لم نقِس جودة Claude بخمسة مصادر لعدم توفر مفتاح API.

## مقارنة نماذج الإجابة المحلية — 25 سبتمبر 2026

جرّبنا ثلاثة نماذج مفتوحة بدل Qwen2.5-1.5B، بالبحث والـPrompt الحاليين نفسيهما ومصدرين للإجابة، على الأسئلة العشرة، على CPU بأربع أنوية. الأحكام مراجعة أولية أجراها المساعد مقابل نصوص المصادر، وليست مراجعة بشرية.

| النموذج | الحجم | صحيحة داخل النطاق | امتناع مناسب خارج النطاق | زمن الإجابة | ملاحظات |
|---|---:|---:|---:|---|---|
| Qwen/Qwen2.5-1.5B-Instruct (الحالي) | 1.5B | 6/8 | 2/2 | من 14 إلى 42 ثانية | السؤال 3 أضاف بندًا غير وارد |
| silma-ai/SILMA-Kashif-2B-Instruct-v1.0 | 2.6B | 7/8 | 1/2 | من 6 إلى 40 ثانية* | أجاب «الفرنسيين» عن كأس العالم 2022 بدل الامتناع؛ خطأ في كلمة بالسؤال 3 |
| Qwen/Qwen2.5-3B-Instruct | 3.1B | 6/8 | 2/2 | من 12 إلى 69 ثانية | السؤال 2 بدأ بـ«نعم» ثم ذكر أنه لا توجد رسوم |
| Qwen/Qwen3-4B-Instruct-2507 (bfloat16) | 4.0B | 6/8 | 2/2 | من 31 إلى 161 ثانية | يذكر المصدر والصفحة دائمًا؛ السؤال 2 بدأ بـ«نعم» |

\* استغرق السؤال الأول 694 ثانية لأن الجهاز كان تحت ضغط ذاكرة من اختبار آخر؛ استبعدناه من المدى.

السؤال 7 ملتبس في جميع النماذج: تجيب «سنة واحدة» دون توضيح نوع الرخصة.

**القرار:** أبقينا Qwen2.5-1.5B نموذجًا محليًا في `app.py`. الفروق سؤال واحد في كل اتجاه على عينة من عشرة أسئلة، وهذا لا يكفي لتفضيل نموذج. SILMA-Kashif الأفضل داخل النطاق، لكن اختراع إجابة خارج النطاق أخطر في تطبيق عام. Qwen3-4B أنظف صياغة لكنه أبطأ بنحو ثلاث مرات على CPU. قيسَ Qwen3-4B بدقة bfloat16 لضيق الذاكرة، وقد تختلف سرعته بدقة float32 على جهاز بذاكرة أكبر. لم نقِس جودة Claude لعدم توفر مفتاح API؛ اختُبر فقط أن المفتاح غير الصالح يعطي رسالة واضحة.

الأقسام التالية تسجل التحقق السابق بإعداد خمسة مرشحين ومصدر واحد، ونُبقيها للمقارنة.

تاريخ الاختبار: 22 سبتمبر 2026. نُفذت الخلايا برمجيًا باستخدام `marimo.App.run()` على CPU وبالنماذج الفعلية، مع `HF_HUB_OFFLINE=1` و`TRANSFORMERS_OFFLINE=1` بعد توفر الأوزان في التخزين المؤقت. لم تُستخدم خدمة inference أو إجابات وهمية.

## ما نُفذ

- فحص marimo وتجميع Python دون أخطاء.
- أول فتح يقرأ البيانات دون تحميل النماذج أو قراءة ملف الإجابات المرجعية.
- البيانات مطابقة بايتًا ببايت لملف المستخدم: 152 صفحة، 577 وحدة سردية/صف جدول، و686 نافذة للاسترجاع.
- تحقق من أن نتائج FAISS IndexFlatIP تطابق Cosine المحسوب بـNumPy ضمن هامش 1e-5.
- استرجاع خمسة مصادر مختلفة، وإعادة ترتيبها فعليًا، وتمرير أفضل مصدر إلى Qwen.
- تنفيذ جدول المقارنة وزري التقييم واختبار خارج النطاق ضمن تشغيل الدفتر.
- حل جميع المراجع اليدوية إلى وحدات نصية فريدة، مع تمييز صف متابعة البلاغ عن صف تقديمه في الصفحة نفسها.
- تحقق من مقاييس الغياب، والمرجع خارج k، ووجود أكثر من مرجع صحيح.
- توليد إجابات للأسئلة العشرة: ثمانية داخل النطاق واثنان خارجه، كلها بالعربية.

## مقارنة البحث الفعلية

| المقياس — 8 أسئلة | قبل إعادة الترتيب | بعد إعادة الترتيب |
|---|---:|---:|
| Hit@1 | 0.625 (5/8) | 0.750 (6/8) |
| Recall@5 | 0.875 | 0.875 |
| MRR@5 | 0.729 | 0.812 |

التقييم هنا على النص أو صف الجدول المرجعي، وليس رقم الصفحة وحده. هذه النتائج ليست نسبة لصحة الإجابات، والتوسيم مقتصر على المراجع التي راجعناها يدويًا.

- السؤال عن وثائق رخصة الذهب والمجوهرات: ارتفع المصدر الصحيح من المرتبة الثالثة إلى الأولى.
- سؤال بيانات حاسبة نهاية الخدمة: بقي المصدر الذي يحتوي المدخلات المطلوبة في المرتبة الثانية؛ `TOP_K = 1` لم يمرره للمولّد.
- سؤال متابعة البلاغ بصياغة HRSD: غاب صف الخطوات المرجعي عن المرشحين الخمسة، فلم تستطع إعادة الترتيب إعادته. الصفحة 87 المسترجعة تذكر قنوات المتابعة، لكنها لا تتضمن خطوات التطبيق المطلوبة.
- ثبات Recall@5 متوقع لأن إعادة الترتيب تغير مواقع المرشحين أنفسهم.

## مراجعة أولية للإجابات أجراها المساعد

| السؤال | النتيجة المرصودة |
|---|---|
| مدة الرخصة المهنية | أجاب سنة واحدة، بما يتفق مع المصدر. |
| رسوم الرخصة | أجاب لا توجد رسوم، بما يتفق مع المصدر. |
| وثائق الرخصة | استرجع النص الصحيح، لكنه أضاف صورة شخصية وشهادة صحية وبنودًا إضافية غير مذكورة؛ إجابة غير موثوقة رغم نجاح البحث. |
| متابعة البلاغ — الصياغة المباشرة | امتنع رغم أن المصدر الصحيح يتضمن خطوات المتابعة؛ إخفاق في التوليد. |
| بيانات حاسبة نهاية الخدمة | امتنع؛ المصدر المختار يشرح الخدمة ولا يورد المدخلات المطلوبة. |
| شروط نقل العمالة المنزلية | أعاد الشروط الأربعة بما يتفق مع المصدر. |
| مدة «رخصة العمل في محلات الذهب» | أجاب سنة واحدة، لكنه لم يوضح الفرق بين الرخصة المهنية المقصودة ورخصة العمل العامة؛ جواب ملتبس. |
| متابعة البلاغ — صياغة HRSD | أضاف خطوات للبحث بالاسم أو الهاتف غير واردة في المصدر المسترجع؛ إجابة غير موثوقة. |
| الفائز بكأس العالم 2022 | امتنع بالعبارة المطلوبة؛ امتناع مناسب لهذه الحالة. |
| وصفة كيكة الشوكولاتة | امتنع بالعبارة المطلوبة؛ امتناع مناسب لهذه الحالة. |

هذه المراجعة أجراها المساعد بمقارنة الإجابات بالمصادر؛ ليست مراجعة بشرية مؤكدة. يستطيع المستخدم مراجعتها داخل قسم Lab 6، وتبدأ حقول مراجعته دون أحكام مسبقة.

لا نُخفي هذه الإخفاقات ولا نستبدلها بإجابات مكتوبة مسبقًا. الغرض التعليمي هو فهم الفرق بين نجاح الاسترجاع وصحة التوليد. ظهور عبارة الامتناع في اختبار الواجهة فحص نصي فقط؛ المراجعة أعلاه تشمل الإجابة كاملة.

## إعادة الاختبار وحدوده

```bash
python -m marimo check rag_marimo.py
python validate_project.py
```

السكربت يعيد تشغيل الحالات ويسجل النتائج الخام في `validation_results.json`. الإجابات المرجعية معزولة عن Prompt. الإعدادات: E5-small، وmmarco-mMiniLMv2 Cross-Encoder، وQwen2.5-1.5B، خمسة مرشحين، ومصدر واحد للإجابة. الاسترجاع وإعادة الترتيب يحتاجان تنزيل نموذجين، والتوليد نموذجًا ثالثًا، أول مرة فقط.

استغرق التشغيل المقاس نحو 237 ثانية على CPU، بما في ذلك الفهرسة وتحميل النماذج من التخزين المؤقت والاختبارات. وقت إجابة السؤال 4 مشمول في تشغيل الدفتر الأول؛ أُعيد استخدام تلك الإجابة في التقرير ولم يُقَس زمن توليدها منفردًا.

- المقارنة عينة تعليمية صغيرة، وليست اختبارًا مستقلًا شاملًا؛ لم نضبط عتبة امتناع على هذه الأسئلة.
- النموذج الصغير قد يضيف معلومات غير واردة أو يمتنع مع وجود الجواب؛ راجع المصادر الفعلية.
- تعطي إعادة الترتيب الأولوية للنافذة المسترجعة، ثم يقرأ مولّد الإجابة النص الأب كاملًا. قد تُقص أزواج السؤال/النافذة الطويلة عند 512 token.
- لم تُنفّذ مراجعة مرئية داخل متصفح أو داخل molab في هذه الجلسة؛ اختُبرت الخلايا ومخرجات الواجهة برمجيًا. لفحص الواجهة محليًا: `python -m marimo edit rag_marimo.py` ثم جرّب السؤال والزّرين وافتح المصادر.
- لم يُختبر على GPU أو Windows، ولم تُراجع نسخة Markdown صفحة بصفحة مقابل PDF.
- لم يُنشر المشروع إلى GitHub. الاتصال متاح، لكن المستودعات الظاهرة لا توفر وجهة كتابة لهذا المشروع؛ ملفات الحزمة جاهزة للرفع إلى مستودعك المخصص.

## تحقق إضافات Lab 6

اختُبرت الإضافات باستخدام `python validate_lab6.py` على نتائج التشغيل المحفوظ أعلاه. لم نعد توليد الأجوبة، ولم تتغير النماذج أو خطوات RAG. أُضيفت وسوم نوع المصدر وصياغة السؤال ونسخة النصوص المسترجعة إلى سجل التشغيل نفسه.

| الشريحة | الأسئلة | مصدر أول صحيح قبل | مصدر أول صحيح بعد |
|---|---:|---:|---:|
| جدول | 3 | 1/3 | 1/3 |
| نص سردي | 5 | 4/5 | 5/5 |
| سؤال مباشر | 6 | 4/6 | 5/6 |
| صياغة بديلة | 2 | 1/2 | 1/2 |

المحوران متداخلان؛ لا نجمع صفوف الجدول باعتبارها 16 سؤالًا. الأسئلة داخل النطاق ثمانية فقط. أسئلة خارج النطاق منفصلة ولا تدخل في مقاييس الاسترجاع.

ما تحقق برمجيًا:

- فتح التقرير وحساب الشرائح وتصنيف الأخطاء والتقرير النهائي دون تحميل أوزان النماذج.
- جميع الإجابات العشر تبدأ دون مراجعة مستخدم. غياب الحكم لا يتحول إلى صفر في الدقة أو حكم بالنجاح.
- خطآن آليان منفصلان: غياب المصدر عن المرشحين في السؤال 8، وعدم اختيار المصدر المتاح للإجابة في السؤال 5.
- المراجعة الأولية للمساعد تشير إلى إضافات غير مدعومة في السؤالين 3 و8، وامتناع رغم كفاية المصدر في السؤال 4، ولبس في السؤال 7. مصدر كل حكم ظاهر.
- إدخال أحكام اختبار في النموذج يغيّر البسط والمقام والتصنيفات، ويستبدل الحكم الأولي بحكم المستخدم دون مضاعفة العدّ. كانت هذه مدخلات تحقق فقط، وليست مراجعات بشرية فعلية منشورة.
- إعادة فتح التقرير دون إدخالات تعيد حالات المراجعة إلى «لم يُراجع».
- نتائج تشغيل جديد بلا مراجعات أولية لا ترث أحكام الإجابات القديمة.
- تقرير التنزيل قابل للتحويل إلى JSON ويحفظ تاريخ التشغيل والأسئلة والإجابات والمرجع ونصوص المصادر ومصدر الحكم.

لم تُجرَ معاينة مرئية في المتصفح أو molab؛ نجح تشغيل خلايا التقرير ومكوناته برمجيًا. للمعاينة افتح الدفتر واضغط زر تقرير Lab 6، وافتح سؤالًا ثم أدخل حكمًا واضغط تطبيق المراجعة ونزّل التقرير. لم نضف Bootstrap أو أسئلة بلغتين.

## فحص بيئة سحابية دون الوصول إلى Hugging Face — 22 سبتمبر 2026

ثُبّتت المتطلبات بالإصدارات المثبتة في `requirements.txt` من PyPI على Python 3.11 وCPU. سياسة الشبكة في هذه البيئة تمنع الوصول إلى huggingface.co، لذلك **لم تُنزّل النماذج الحقيقية ولم يُعد توليد الإجابات**. نتائج `validation_results.json` والأرقام أعلاه لم تتغير ولم يُعد التحقق منها هنا.

ما نُفذ:

- `python -m marimo check rag_marimo.py` دون أخطاء.
- `python validate_lab6.py` نجح.
- تشغيل كل خلايا الدفتر برمجيًا بنماذج محلية صغيرة بأوزان عشوائية (للتحقق من مسار الكود فقط؛ المقاييس والإجابات الناتجة بلا معنى): لا أخطاء تشغيل.
- فتح الواجهة عبر `marimo run` في Chromium دون أخطاء JavaScript، وتشغيل تقرير Lab 6 وزر التجهيز دون اتصال.

ما تغيّر:

- إذا تعذر تنزيل نموذج أو قراءته تظهر رسالة عربية واضحة بدل تتبع أخطاء Python.
- رسائل الحالة وجداول النتائج تُعرض من اليمين إلى اليسار مثل بقية الدفتر.
- `validate_project.py` يتوقف برسالة واضحة إذا لم تُحمَّل النماذج، ولم يعد يفترض أن رقم سؤال الواجهة هو 4.

لم تتغير خطوات الاسترجاع أو إعادة الترتيب أو التوليد. (غيّرها لاحقًا إعداد إعادة ترتيب 20 مرشحًا الموصوف أعلى هذا الملف.)
